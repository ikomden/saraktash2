
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отбор = Неопределено;
	Если Параметры.Свойство("Отбор", Отбор) Тогда
		Если Отбор.Свойство("НаСайте") И Отбор.НаСайте = Истина Тогда
			Заголовок = "Мастерицы для проверки";
			Элементы.Список.ТолькоПросмотр = Истина;
		КонецЕсли;
		Если Отбор.Свойство("ДляПроизводства") И Отбор.ДляПроизводства = Истина Тогда
			Заголовок = "Вязальщицы в Саракташе"; 			
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
